# Migrations Folder

This folder is intentionally empty after baseline consolidation.

- Current schema source of truth: `database/init/*.sql`
- Keep `.gitkeep` so the directory remains in git.
- Add new migration files here only for schema changes created after the current baseline.
